name: oh_my_flutter
description: Flutter/Dart superpower utils — extensions, helpers, interceptors, and reusable patterns for everyday Flutter development.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  dio: ^5.9.2
  flutter:
    sdk: flutter
  internet_connection_checker_plus: ^3.1.0
  meta: ^1.18.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^7.0.0
