// Autogenerated from Pigeon (v28.0.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class DeviceLocalePigeonError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "DeviceLocalePigeonError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? DeviceLocalePigeonError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(Swift.type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

enum DeviceLocalePigeonInternal {
  static func isNullish(_ value: Any?) -> Bool {
    guard let innerValue = value else {
      return true
    }

    if case Optional<Any>.some(Optional<Any>.none) = value {
      return true
    }

    return innerValue is NSNull
  }
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}


private class DeviceLocalePigeonCodecReader: FlutterStandardReader {
}

private class DeviceLocalePigeonCodecWriter: FlutterStandardWriter {
}

private class DeviceLocalePigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return DeviceLocalePigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return DeviceLocalePigeonCodecWriter(data: data)
  }
}

class DeviceLocalePigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = DeviceLocalePigeonCodec(readerWriter: DeviceLocalePigeonCodecReaderWriter())
}

/// Defines native operations for device locale preferences.
///
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol DeviceLocaleHostApi {
  /// Returns the configured region identifier when available.
  func getCountry() throws -> String?
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class DeviceLocaleHostApiSetup {
  static var codec: FlutterStandardMessageCodec { DeviceLocalePigeonCodec.shared }
  /// Sets up an instance of `DeviceLocaleHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: DeviceLocaleHostApi?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    /// Returns the configured region identifier when available.
    let getCountryChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.oh_my_flutter.DeviceLocaleHostApi.getCountry\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      getCountryChannel.setMessageHandler { _, reply in
        do {
          let result = try api.getCountry()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      getCountryChannel.setMessageHandler(nil)
    }
  }
}
