/// Physical direction in which a marquee moves its children.
enum MarqueeDirection {
  /// Moves the child strip toward the left edge.
  left,

  /// Moves the child strip toward the right edge.
  right,

  /// Moves the child strip toward the top edge.
  top,

  /// Moves the child strip toward the bottom edge.
  down,
}
